# To serve project:
```
npm install -g serve
serve -s build
```
# To adjust port:
Use the -l flag like this:
```
serve -s build -l 4000
```
See more here:
https://create-react-app.dev/docs/deployment/
